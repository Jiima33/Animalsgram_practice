<!--コメント表示　[非同期通信化]-->

<h4 class="col-md-5 col-lg-4 col-xl-3 py-1 mt-5 back-text shadow">
  <strong>
    コメント一覧
  </strong>
</h4>

  <table class="table shadow mb-5 mx-auto back-background col-xl-12 col-lg-13 col-md-12 comment-table">
    <% if post_image.post_comments.present? %>
      <% post_image.post_comments.each do |post_comment| %>
        <tbody>
          <tr>
            <td>
              <%= image_tag post_comment.user.get_profile_image(300,300), class: "rounded-circle", :size => '100' %><br>
              [ 会員名 ]
              <%= link_to user_path(post_comment.user.id) do %>
                <!--コメントをした会員名(パソコン、タブレットの表示)-->
                <div class="d-none d-sm-block">
                  <%= post_comment.user.name %>
                </div>

            <!--コメント内容-->
　　　　　  <td class="text-word-break">
　　　　　    <div class="comment-text-indent">
　　　　　      <%= post_comment.comment %>
　　　　　    </div>
　　　　　    
　　　　　   <!--削除ボタン-->
              <% if post_comment.user == current_user %>
                <div class="row justify-content-end mr-2 mt-5 pt-5">
                  <%= link_to "削除", post_image_post_comment_path(post_comment.post_image, post_comment), method: :delete, data: {confirm: "本当に削除しますか？"}, remote: true, class: "btn destroy-btn" %>
                </div>
              <% end %>
              <% end %>
　　　　　  </td>
          </tr>
        </tbody>
      <% end %>
    <% end %>
  </table>