$("#comment_area").html("<%= j(render "post_comments/comment_index", post_image :@post_image), comment :@comment %>");
$("#comment_count").html("<%= @post_image.post_comments.count %>");
$("textarea").val('');
